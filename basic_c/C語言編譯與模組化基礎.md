# C 語言編譯與模組化基礎

> 📅 建立日期：2026-02-09  
> 🎯 目標：建立正確的 C 語言編譯流程觀念，避免常見錯誤

---

## 目錄

1. [extern 關鍵字](#1-extern-關鍵字)
2. [宣告 vs 定義](#2-宣告-vs-定義)
3. [編譯流程：編譯 vs 連結](#3-編譯流程編譯-vs-連結)
4. [為什麼不能 #include ".c" 檔案](#4-為什麼不能-include-c-檔案)
5. [正確的專案模組化結構](#5-正確的專案模組化結構)
6. [Include Guard 詳解](#6-include-guard-詳解)
7. [面試常見問題](#7-面試常見問題)
8. [快速記憶口訣](#8-快速記憶口訣)

---

## 1. extern 關鍵字

### 核心概念

`extern` 是用來**宣告**一個變數或函式「存在於其他編譯單元（其他 `.c` 檔案）」，但**不分配記憶體**。

### 基本用法

```c
// ===== file_a.c =====
int counter = 0;          // ✅ 定義：分配記憶體

// ===== file_b.c =====
extern int counter;       // ✅ 宣告：告訴編譯器「counter 存在於別處」
counter++;                // 可以操作
```

### 三大用途

#### 用途 1：跨檔案共享全域變數（最常見）

```c
// config.c
int debug_level = 1;      // 定義

// main.c
extern int debug_level;   // 宣告，使用 config.c 的變數
```

#### 用途 2：在標頭檔 (.h) 中宣告全域變數

```c
// config.h
extern int debug_level;   // 宣告（放在 .h）

// config.c
#include "config.h"
int debug_level = 1;      // 定義（放在 .c）
```

> ⚠️ **重要**：在 `.h` 中「定義」變數會導致 **multiple definition error**！

#### 用途 3：存取 const 變數

```c
// constants.c
extern const int MAX_SIZE = 100;  // ✅ 必須加 extern 強制 external linkage

// main.c
extern const int MAX_SIZE;        // ✅ 可以存取
```

### 陣列的 extern 宣告

```c
// main.c
int data[5] = {2, 3, 5, 7, 9};   // 定義

// utils.c
extern int data[];               // ✅ 宣告時可以省略陣列大小
// 或
extern int data[5];              // ✅ 寫明大小也可以（但要確保一致）
```

---

## 2. 宣告 vs 定義

這是理解 `extern` 的前提，**必須徹底搞懂**！

| 概念 | 說明 | 是否分配記憶體 | 範例 |
|------|------|----------------|------|
| **定義 (Definition)** | 變數「真正存在」，分配記憶體 | ✅ 是 | `int data[5] = {1,2,3,4,5};` |
| **宣告 (Declaration)** | 告訴編譯器「某東西存在」 | ❌ 否 | `extern int data[];` |

### 關鍵規則

- **定義只能有一個**（不然會 multiple definition error）
- **宣告可以有很多個**（每個需要使用的檔案都要宣告）

---

## 3. 編譯流程：編譯 vs 連結

### 兩階段流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        編譯流程                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   main.c ──┐                                                    │
│            ├──▶ [編譯器] ──▶ main.o ──┐                         │
│            │                          │                         │
│  utils.c ──┘                          ├──▶ [連結器] ──▶ program │
│            ├──▶ [編譯器] ──▶ utils.o ─┘                         │
│            │                                                    │
│                                                                 │
│   階段 1：編譯                  階段 2：連結                      │
│   (每個 .c 獨立處理)           (組合所有 .o)                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 階段說明

| 階段 | 執行者 | 輸入 | 輸出 | 做什麼事 |
|------|--------|------|------|----------|
| **編譯** | 編譯器 (gcc -c) | `.c` 檔案 | `.o` 檔案 | 語法檢查、轉成機器碼 |
| **連結** | 連結器 (gcc) | 多個 `.o` | 可執行檔 | 解決函式呼叫位址 |

### 實際命令

```bash
# 分別編譯（產生 .o）
gcc -c main.c -o main.o
gcc -c utils.c -o utils.o

# 連結（產生可執行檔）
gcc main.o utils.o -o program

# 或一次完成
gcc main.c utils.c -o program
```

### 編譯時期 vs 連結時期

#### 編譯 main.c 時：

```c
void displayData(void);   // 函式宣告（原型）

int main() {
    displayData();        // 編譯器：「好，我知道有這函式，先記住」
}
```

編譯器產生的 `main.o` 內容類似：

```
呼叫 displayData → 地址 = ???（待定）
```

#### 連結時期：

連結器會：
1. 掃描所有 `.o` 檔案
2. 找到 `utils.o` 裡面的 `displayData` 實作
3. 把「待定」的地址填上正確值

---

## 4. 為什麼不能 #include ".c" 檔案

### ❌ 錯誤做法

```c
// main.c
#include "utils.c"   // ❌ 永遠不要這樣做！
```

### 為什麼技術上可行，但絕對不該做？

| 問題 | 說明 |
|------|------|
| **破壞模組化** | 所有程式碼變成一個巨大檔案 |
| **編譯時間災難** | 無法增量編譯，每次都要全部重編 |
| **容易出錯** | 如果不小心也編譯了 utils.c 會 multiple definition |
| **違反慣例** | 別人看你的 code 會完全看不懂 |

### 增量編譯的價值

#### 模組化（多個 .o）

```
修改 utils.c 一行 → 只重編譯 utils.o（幾秒）
```

#### #include ".c"（一個大 .o）

```
修改 utils.c 一行 → 重編譯整個 main.o（幾分鐘）
```

在大型專案（幾十萬行程式碼）中，這會讓編譯時間從**幾秒變成幾十分鐘**！

---

## 5. 正確的專案模組化結構

### 標準目錄結構

```
my_project/
├── Makefile           # 自動化編譯腳本
├── include/           # 所有標頭檔
│   └── data_utils.h
├── src/               # 所有原始碼
│   ├── main.c
│   └── data_utils.c
└── build/             # 編譯產物（.o 檔案）
```

### 三種檔案的職責

| 檔案類型 | 職責 | 應該包含 | 不應該包含 |
|----------|------|----------|------------|
| **`.h` 標頭檔** | 介面 | 函式宣告、extern 變數、typedef、巨集 | 函式實作、變數定義 |
| **`.c` 實作檔** | 實作 | 函式定義、static 私有函式、全域變數 | 其他模組的私有細節 |
| **`main.c`** | 進入點 | main() 函式、呼叫各模組 | 過多的業務邏輯 |

### 完整範例

#### `include/data_utils.h`（標頭檔）

```c
#ifndef DATA_UTILS_H    // Include guard
#define DATA_UTILS_H

// ===== 外部變數宣告 =====
extern int data[];       // 宣告，不定義

// ===== 函式原型 =====
void displayData(void);
void manipulateData(void);

#endif // DATA_UTILS_H
```

#### `src/data_utils.c`（實作檔）

```c
#include <stdio.h>
#include "data_utils.h"   // ✅ include 自己的標頭檔

// ===== 函式實作 =====
void displayData(void) {
    int x;
    printf("Data Set:\n");
    for(x = 0; x < 5; x++)
        printf("%2d\t", data[x]);
    putchar('\n');
}

void manipulateData(void) {
    int x;
    for(x = 0; x < 5; x++)
        data[x] *= 2;
    puts("* Data manipulated *");
}
```

#### `src/main.c`（主程式）

```c
#include <stdio.h>
#include "data_utils.h"   // ✅ 只 include 標頭檔

// ===== 全域變數定義 =====
int data[5] = { 2, 3, 5, 7, 9 };

// ===== 主程式 =====
int main(void) {
    displayData();
    manipulateData();
    displayData();
    return 0;
}
```

### Makefile 範例

```makefile
CC = gcc
CFLAGS = -Wall -I./include
SRC = src/main.c src/data_utils.c
OBJ = $(SRC:.c=.o)
TARGET = program

all: $(TARGET)

$(TARGET): $(OBJ)
	$(CC) $(OBJ) -o $(TARGET)

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(OBJ) $(TARGET)
```

---

## 6. Include Guard 詳解

### 什麼是 Include Guard

```c
#ifndef DATA_UTILS_H     // 如果未定義
#define DATA_UTILS_H     // 就定義它

// ... 標頭檔內容 ...

#endif // DATA_UTILS_H
```

### 為什麼需要它？

防止**同一個編譯單元**中，同一個標頭檔被 include 多次。

### 菱形依賴問題

```
       main.c
      /      \
     a.h     b.h
      \      /
     data_utils.h  ← 沒有 guard 會被 include 兩次
```

```c
// a.h
#include "data_utils.h"

// b.h  
#include "data_utils.h"

// main.c
#include "a.h"           // 間接 include data_utils.h
#include "b.h"           // 又間接 include data_utils.h
```

#### 沒有 Include Guard 的後果

```c
// main.c 展開後
extern int data[];           // 第一次
void displayData(void);
void manipulateData(void);

extern int data[];           // ❌ 重複！
void displayData(void);      // ❌ 重複！
void manipulateData(void);   // ❌ 重複！
```

#### 有 Include Guard 的行為

1. **第一次 include**：`DATA_UTILS_H` 未定義 → 進入 → 定義 macro → 處理內容
2. **第二次 include**：`DATA_UTILS_H` 已定義 → **跳過整個檔案**

### ⚠️ 重要澄清：不同編譯單元

> **Include Guard 只在「同一個編譯單元」內生效！**

`main.c` 和 `utils.c` 是**不同的編譯單元**，它們各自完整處理自己的 `#include`。

```bash
gcc -c main.c        # 編譯單元 1：完整處理 data_utils.h
gcc -c data_utils.c  # 編譯單元 2：完整處理 data_utils.h
```

這是**正確且必要的**！每個 `.c` 都需要看到完整的宣告。

---

## 7. 面試常見問題

### Q1: extern 和全域變數有什麼關係？

> 全域變數預設就具有 **external linkage**，`extern` 是用來在**其他檔案中存取**該變數的宣告方式。

### Q2: 以下程式碼有什麼問題？

```c
// header.h
int global_var = 10;  // ❌ 錯誤寫法
```

> 在標頭檔中**定義**變數，會導致每個 include 該標頭檔的 `.c` 都產生一份定義，造成 **multiple definition error**。
>
> 正確做法：
> ```c
> // header.h
> extern int global_var;  // ✅ 宣告
> 
> // source.c
> int global_var = 10;    // ✅ 定義
> ```

### Q3: 為什麼標頭檔需要 Include Guard？

> 防止同一個標頭檔在同一編譯單元中被重複 include，造成重複宣告錯誤。

### Q4: static 和 extern 有什麼差別？

| 關鍵字 | Linkage | 可見範圍 |
|--------|---------|----------|
| `extern` | External | 整個程式（跨檔案） |
| `static` | Internal | 只在當前檔案 |

```c
// file1.c
static int private_var = 10;  // 只有 file1.c 看得到
int public_var = 20;          // 其他檔案也看得到

// file2.c
extern int public_var;        // ✅ 可以存取
extern int private_var;       // ❌ 連結錯誤！
```

---

## 8. 快速記憶口訣

### 模組化三原則

> **「標頭管宣告，原始檔管定義，main 管呼叫」**

```
.h  → 「有什麼」（介面）
.c  → 「怎麼做」（實作）  
main.c → 「用起來」（組裝）
```

### extern 用途

> **「extern = 跨檔案的橋樑」**

### Include Guard

> **「一個編譯單元中，標頭檔只進來一次」**

### 絕不 #include ".c"

> **「.h 用來 include，.c 用來編譯」**

---

## 常見錯誤速查表

| 錯誤訊息 | 原因 | 解法 |
|----------|------|------|
| `undefined reference to 'xxx'` | 有宣告但沒定義 | 確保某個 `.c` 檔有定義 |
| `multiple definition of 'xxx'` | 多個檔案都定義了 | 只在一個 `.c` 檔定義，其他用 `extern` |
| `redefinition of 'xxx'` | 同一檔案重複定義 | 加 Include Guard |
| `unknown type name 'xxx'` | 沒有 include 標頭檔 | 加上 `#include "xxx.h"` |

---

## 參考資料

- 範例原始碼：`Ex_Files_Advanced_C_Programming_2022/Exercise Files/CH03/`
- 相關主題：`static` 關鍵字、`extern "C"`（C++ 混用）

